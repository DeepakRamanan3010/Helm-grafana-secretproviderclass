apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: {{ .Values.secretStore.name }}
spec:
  provider:
    aws:
      service: SecretsManager
      role: {{ .Values.secretProviderClass.roleArn }}
      region: {{ .Values.secretProviderClass.region }}

